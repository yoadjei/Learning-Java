📌 Analyzes an array of ratings (1-5) and counts the frequency of each rating, then displays the result.
The program initializes an array with predefined responses, processes them using a frequency array, and efficiently counts occurrences using a loop. The output presents a structured frequency distribution of the given ratings.
